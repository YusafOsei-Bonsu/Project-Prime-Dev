var mongo = require('mongodb').MongoClient;

/* 
//mongo.connect(MongoDB URI goes here, function(error, db) {
  if(!error) {
    alert("Connected to MongoDB database woo!");
  } else {
    alert("Something went wrong bro.. this sucks bro.");
  }
});
*/
